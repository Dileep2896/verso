"""Sponsor integrations. Each hides behind an interface with a local fake so the
demo runs offline: two days before a deadline is the wrong time to discover a
hosted API is rate limited, and a demo that cannot run offline will fail in the
room.
"""
